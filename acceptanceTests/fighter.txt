; Fighter-specific behavior tests.

;===============================================================
; Fighter consumes fuel when moving over non-city terrain.
;===============================================================
GIVEN game map
  F~=
F has fuel 32.
GIVEN F is waiting for input.

WHEN the player presses D and the game advances until F is waiting for input.

THEN F is at =.
THEN F has fuel 30.
THEN the attention message contains :hit-edge.

;===============================================================
; Fighter refuels at player city.
;===============================================================
GIVEN game map
  FO
F has fuel 10.
GIVEN F is waiting for input.

WHEN the player presses d.

THEN at the next step O has one fighter in its airport and there is no fighter on the map.

;===============================================================
; Fighter bingo wakes when fuel low and city nearby.
;===============================================================
GIVEN game map
  OF
F is sentry with fuel 9.

WHEN a new round starts and F is waiting for input.

THEN F has mode awake.
and the attention message for F contains :fighter-bingo.

;===============================================================
; Fighter out of fuel wakes.
;===============================================================
GIVEN game map
  #F
F is sentry with fuel 2.

WHEN a new round starts.

THEN F wakes up and asks for input,
and the attention message contains :fighter-out-of-fuel.

;===============================================================
; Fighter crashes when fuel reaches zero.
;===============================================================
GIVEN game map
  #F
F is sentry with fuel 1.

WHEN a new round starts.

THEN there is no F on the map.
THEN the error message contains :fighter-crashed.

;===============================================================
; Fighter lands on carrier.
;===============================================================
GIVEN game map
  FC
C has no fighters.
GIVEN F is waiting for input.

WHEN the player presses d.

THEN At the next round C has one fighter aboard
and there is no F on the map.

;===============================================================
; Fighter launched from airport.
;===============================================================
GIVEN game map
  O%
GIVEN O has one fighter in its airport.
GIVEN F is waiting for input.

WHEN the player presses d.

THEN after one step there is an F at %.
and O has no fighters.

;===============================================================
; Fighter attention shows fuel.
;===============================================================
GIVEN game map
  F#
F has fuel 20.
WHEN F is waiting for input.

THEN the attention message contains "fuel:20".

;===============================================================
; Fighter speed is 8 per round.
;===============================================================
GIVEN game map
  F~~~~~~~~=~
GIVEN F is waiting for input.

WHEN the player presses D.

THEN at next round F will be at =.

;===============================================================
; Fighter with out-of-bounds target stays on-map at edge.
;===============================================================
GIVEN game map
  F%#
GIVEN F has mode moving.
GIVEN F has target [99 99].

WHEN a new round starts and F is waiting for input.

THEN F is at [2 0].

;===============================================================
; Computer fighter hops over one friendly army.
; The fighter at [6 0] moves toward a distant target, hopping
; over the computer army at [7 0] and landing at [8 0], then
; continuing for a total of 8 steps to reach [14 0].
;===============================================================
GIVEN game map
  X#####fa################=
GIVEN computer map
  X#####fa################=
f has fuel 30.
f has flight-target-site [24 0].

WHEN computer fighter f is processed.

THEN f is at [14 0].
THEN f has fuel 22.

;===============================================================
; Computer fighter hops over multiple friendly armies.
; Three computer armies block the path between [7 0] and [9 0].
; The fighter hops all three in a single move, consuming 4 steps,
; then continues forward for 4 more single steps.
;===============================================================
GIVEN game map
  X#####faaa##############=
GIVEN computer map
  X#####faaa##############=
f has fuel 30.
f has flight-target-site [24 0].

WHEN computer fighter f is processed.

THEN f is at [14 0].
THEN f has fuel 22.

;===============================================================
; Computer fighter hop costs fuel per cell hopped.
; With fuel 30, the fighter takes 8 steps (including a 4-cell
; hop over 3 armies costing 4 fuel). Each cell traversed costs
; 1 fuel regardless of hop, so 8 total fuel spent.
;===============================================================
GIVEN game map
  X#####faaa##############=
GIVEN computer map
  X#####faaa##############=
f has fuel 28.
f has flight-target-site [24 0].

WHEN computer fighter f is processed.

THEN f has fuel 20.

;===============================================================
; Computer fighter prefers unexplored cells during regular leg.
; Fighter at [2 2] navigating toward city at [9 4] with plenty
; of fuel. Row 0 is unexplored on computer-map. The fighter
; should detour toward row 1 (bordering unexplored row 0),
; revealing row-0 cells via visibility updates.
;===============================================================
GIVEN game map
  ##########
  ##########
  ##f#######
  ##########
  #########X
GIVEN computer map
  ----------
  ##########
  ##f#######
  ##########
  #########X
f has fuel 30.
f has flight-target-site [9 4].
f has flight-origin-site [0 0].
f has flight-mode regular.

WHEN computer fighter f is processed.

THEN computer can see [2 0].
