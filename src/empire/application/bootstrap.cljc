;; mutation-tested: no
(ns empire.application.bootstrap
  "Application initialization.")

(defn initialize-default-services!
  "Initializes application services. No-op now that all wiring uses direct requires."
  []
  true)
